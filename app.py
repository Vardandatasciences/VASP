# Define a function that takes two numbers as parameters
# and returns their sum.
def add_two_numbers(number1, number2):
    return number1 + number2  # Adds the two numbers and returns the result

if __name__ == "__main__": 
    # Call the function with two numbers (5 and 6) and print the result
    print(add_two_numbers(5, 6))  # Output will be 11
